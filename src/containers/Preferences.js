import React, { Component } from 'react';

class Preferences extends Component {
  render() {
    return (
      <div>
        PREFERENCES
      </div>
    );
  }
}

export default Preferences;


// 1. start_date_to_display - date(could be a string)
// 2. daily_reminder_on - boolean
// 3. If(daily_reminder_on) daily_reminder_time — date(could be a string)
// 4. sounds_on - boolean
// 5. launch_at_login - boolean
// 6. Window_width - number